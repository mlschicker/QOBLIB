<!-- qoblib-validation -->
## 🤖 QOBLIB submission validation

{{VERDICT}}

{{RESULTS}}

---
_Policy: solution files must always be **valid**; **feasibility** is checked only when the submission declares it (`# Feasible Runs` > 0 or blank), and **optimality** only when it asserts a proven optimum (`Optimality Bound` = `Best Objective Value`). See [`misc/ci/CHECKER_CONTRACT.md`]({{DOC_URL}})._
